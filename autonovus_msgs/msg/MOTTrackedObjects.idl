#include "autonovus_msgs/msg/MOTTrackedObject.idl"
#include "std_msgs/msg/Header.idl"

module autonovus_msgs {
  module msg {
    @verbatim (language="comment", text=
      " This is the output of object tracking and the input to prediction.")
    struct MOTTrackedObjects {
      std_msgs::msg::Header header;
      sequence<autonovus_msgs::msg::MOTTrackedObject> objects;
    };
  };
};
